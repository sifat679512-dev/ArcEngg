# Sprint 1 Client

Vite + React minimal UI for role-based login/signup.

- Admin: username `admin`, password `12345`
- Client/Service provider: use signup then login

Configure API base by setting `VITE_API_BASE` in a `.env` file if needed.
